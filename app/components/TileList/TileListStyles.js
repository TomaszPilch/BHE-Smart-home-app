// @flow
import { StyleSheet } from 'react-native'

export default () =>
  StyleSheet.create({
    scrollView: {
      flexWrap: 'wrap',
      flexDirection: 'row',
      justifyContent: 'space-around',
    },
  })
